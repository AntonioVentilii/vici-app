<script lang="ts">
	import InviteLanding from '$lib/components/arena/InviteLanding.svelte';

	// `/i/[code]` and `/join/[code]` are the two canonical invite URLs — both carry the
	// referral code in their slug and render the shared landing, so they behave identically.
</script>

<InviteLanding />
